import Layout from "../components/Layout";
import { places } from "../lib/seeds/seedHelpers";

const Detail = () => {
  return (
    <Layout title="見どころ一覧">
      <h1>詳細ページ</h1>
    </Layout>
  );
};

export default Detail;
